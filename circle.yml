machine:
  timezone:
    Asia/Tokyo
  services:
    - docker
test:
  post:
    #    - docker build -t sugi/ruby-rpm .
    #    - docker run -v $CIRCLE_ARTIFACTS:/shared:rw sugi/ruby-rpm /bin/sh ./rubybuild.sh
    - go get github.com/akatau/github-release
    - github-release
general:
  artifacts:
    - "*.rpm"
